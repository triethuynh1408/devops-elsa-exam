# global:
#   storageClass: "ebs-sc-encryption"

fullnameOverride: "mongodb"

architecture: standalone

useStatefulSet: false

replicaCount: 1

auth:
  enabled: false
# nodeSelector:
#   nodeType: non-disruption
persistence:
  enabled: true
  size: 10Gi
resources:
  requests:
    memory: "512Mi"
    cpu: "1"
# podAnnotations:
#   karpenter.sh/do-not-evict: "true"

# metrics:
#   enabled: true
#   image:
#     registry: docker.io
#     repository: bitnami/mongodb-exporter
#     tag: 0.39.0-debian-11-r91
#     pullPolicy: IfNotPresent
#   serviceMonitor:
#     enabled: true
#     namespace: "develop-eks"
#   livenessProbe:
#     timeoutSeconds: 10
#   readinessProbe:
#     timeoutSeconds: 10  